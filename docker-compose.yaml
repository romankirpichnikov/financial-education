services:
  postgres:
    image: postgres:16
    restart: always
    ports:
      - 5499:5432
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      PGDATA: "/var/lib/postgresql/data"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: {}
